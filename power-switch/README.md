Power Switch Watcher for the Buffalo LinkStation
===

The script `power-switch-watch` should be placed in `/usr/local/sbin` and the
service unit file `power-switch-watch.service` should be placed in
`/etc/systemd/system`.

(TODO)
